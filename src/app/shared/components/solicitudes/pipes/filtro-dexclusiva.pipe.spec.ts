import { FiltroDexclusivaPipe } from './filtro-dexclusiva.pipe';

describe('FiltroDexclusivaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDexclusivaPipe();
    expect(pipe).toBeTruthy();
  });
});
